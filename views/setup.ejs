<section class="auth-page">
  <div class="auth-card">
    <div class="auth-icon">🛠</div>
    <h1>First-time setup</h1>
    <p class="auth-sub">Create the Owner account for this KitsuNexus server. This runs once — after this, sign-in happens on the Log in page. Public registration isn't enabled yet.</p>

    <% if (error) { %><div class="pair-result err" style="max-width:none;margin-bottom:14px"><%= error %></div><% } %>

    <% if (zitadelEnabled) { %>
      <a class="btn sso" href="/auth/zitadel/login">🛡 Become Owner via NekoSuneVR SSO</a>
      <div class="auth-divider">or</div>
    <% } %>

    <form method="post" action="/setup" class="auth-form">
      <input type="text" name="displayName" placeholder="Display name" autocomplete="name" autofocus />
      <input type="email" name="email" placeholder="Email" autocomplete="email" required />
      <input type="password" name="password" placeholder="Password (min 8 characters)" autocomplete="new-password" required />
      <input type="password" name="confirmPassword" placeholder="Confirm password" autocomplete="new-password" required />
      <button class="btn primary" type="submit">Create Owner account</button>
    </form>
  </div>
</section>
