# Dependencies
node_modules/
*.log

# Runtime data (authorized-guilds whitelist) — persisted via a mounted
# volume in production, not committed
data/

# Secrets — NEVER commit
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
